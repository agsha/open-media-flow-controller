/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "jmfcNamespaceRtspClientTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement jmfcNamespaceRtspClientTable get routines.
 * TODO:240:M: Implement jmfcNamespaceRtspClientTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table jmfcNamespaceRtspClientTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientTable is subid 4 of jmfcNamespace.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4, length: 12
*/

/*
 * ---------------------------------------------------------------------
 * * TODO:200:r: Implement jmfcNamespaceRtspClientTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
jmfcNamespaceRtspClientTable_indexes_set_tbl_idx
    (jmfcNamespaceRtspClientTable_mib_index * tbl_idx,
     char *jmfcNamespaceName_val_ptr, size_t jmfcNamespaceName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:jmfcNamespaceRtspClientTable:jmfcNamespaceRtspClientTable_indexes_set_tbl_idx","called\n"));

    /*
     * jmfcNamespaceName(2)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/h 
     */
    tbl_idx->jmfcNamespaceName_len = sizeof(tbl_idx->jmfcNamespaceName)/sizeof(tbl_idx->jmfcNamespaceName[0]); /* max length */
    /*
     * make sure there is enough space for jmfcNamespaceName data
     */
    if ((NULL == tbl_idx->jmfcNamespaceName) ||
        (tbl_idx->jmfcNamespaceName_len <
         (jmfcNamespaceName_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->jmfcNamespaceName_len = jmfcNamespaceName_val_ptr_len;
    memcpy(tbl_idx->jmfcNamespaceName, jmfcNamespaceName_val_ptr,
           jmfcNamespaceName_val_ptr_len *
           sizeof(jmfcNamespaceName_val_ptr[0]));
    

    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
jmfcNamespaceRtspClientTable_indexes_set
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     char *jmfcNamespaceName_val_ptr, size_t jmfcNamespaceName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:jmfcNamespaceRtspClientTable:jmfcNamespaceRtspClientTable_indexes_set","called\n"));

    if (MFD_SUCCESS !=
        jmfcNamespaceRtspClientTable_indexes_set_tbl_idx(&rowreq_ctx->
                                                         tbl_idx,
                                                         jmfcNamespaceName_val_ptr,
                                                         jmfcNamespaceName_val_ptr_len))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if (0 !=
        jmfcNamespaceRtspClientTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientTable_indexes_set */


/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientRequests
 * jmfcNamespaceRtspClientRequests is subid 1 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.1
 * Description:
Total number of RTSP client requests received for
		this namespace.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientRequests data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientRequests_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientRequests_get(jmfcNamespaceRtspClientTable_rowreq_ctx
                                    * rowreq_ctx,
                                    U64 *
                                    jmfcNamespaceRtspClientRequests_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientRequests_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientRequests data.
 * get (* jmfcNamespaceRtspClientRequests_val_ptr ).low and (* jmfcNamespaceRtspClientRequests_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientRequests_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientRequests.high;
    (*jmfcNamespaceRtspClientRequests_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientRequests.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientRequests_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientResponses
 * jmfcNamespaceRtspClientResponses is subid 2 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.2
 * Description:
Total number of RTSP Client responses that
		was sent.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientResponses data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientResponses_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientResponses_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientResponses_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientResponses_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientResponses data.
 * get (* jmfcNamespaceRtspClientResponses_val_ptr ).low and (* jmfcNamespaceRtspClientResponses_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientResponses_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientResponses.high;
    (*jmfcNamespaceRtspClientResponses_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientResponses.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientResponses_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientCacheHit
 * jmfcNamespaceRtspClientCacheHit is subid 3 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.3
 * Description:
Total number of requests that resulted in a cache
		hit, i.e. the object was served from the cache.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientCacheHit data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientCacheHit_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientCacheHit_get(jmfcNamespaceRtspClientTable_rowreq_ctx
                                    * rowreq_ctx,
                                    U64 *
                                    jmfcNamespaceRtspClientCacheHit_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientCacheHit_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientCacheHit data.
 * get (* jmfcNamespaceRtspClientCacheHit_val_ptr ).low and (* jmfcNamespaceRtspClientCacheHit_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientCacheHit_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientCacheHit.high;
    (*jmfcNamespaceRtspClientCacheHit_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientCacheHit.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientCacheHit_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientCacheMiss
 * jmfcNamespaceRtspClientCacheMiss is subid 4 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.4
 * Description:
Total number of client requests that resulted in a
		cache miss, i.e. the object was not found in cache
		and was served from the origin-server.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientCacheMiss data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientCacheMiss_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientCacheMiss_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientCacheMiss_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientCacheMiss_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientCacheMiss data.
 * get (* jmfcNamespaceRtspClientCacheMiss_val_ptr ).low and (* jmfcNamespaceRtspClientCacheMiss_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientCacheMiss_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientCacheMiss.high;
    (*jmfcNamespaceRtspClientCacheMiss_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientCacheMiss.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientCacheMiss_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientCachePartialHit
 * jmfcNamespaceRtspClientCachePartialHit is subid 5 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.5
 * Description:
Number of requests that resulted in a partial hit in cache,
		and rest of the data was a cache miss.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientCachePartialHit data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientCachePartialHit_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientCachePartialHit_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientCachePartialHit_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientCachePartialHit_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientCachePartialHit data.
 * get (* jmfcNamespaceRtspClientCachePartialHit_val_ptr ).low and (* jmfcNamespaceRtspClientCachePartialHit_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientCachePartialHit_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientCachePartialHit.high;
    (*jmfcNamespaceRtspClientCachePartialHit_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientCachePartialHit.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientCachePartialHit_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientInBytes
 * jmfcNamespaceRtspClientInBytes is subid 6 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.6
 * Description:
Number of bytes received from RTSP Clients
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientInBytes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientInBytes_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientInBytes_get(jmfcNamespaceRtspClientTable_rowreq_ctx
                                   * rowreq_ctx,
                                   U64 *
                                   jmfcNamespaceRtspClientInBytes_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientInBytes_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientInBytes data.
 * get (* jmfcNamespaceRtspClientInBytes_val_ptr ).low and (* jmfcNamespaceRtspClientInBytes_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientInBytes_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientInBytes.high;
    (*jmfcNamespaceRtspClientInBytes_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientInBytes.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientInBytes_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientOutBytes
 * jmfcNamespaceRtspClientOutBytes is subid 7 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.7
 * Description:
Number of bytes sent to RTSP Clients
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientOutBytes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientOutBytes_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientOutBytes_get(jmfcNamespaceRtspClientTable_rowreq_ctx
                                    * rowreq_ctx,
                                    U64 *
                                    jmfcNamespaceRtspClientOutBytes_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientOutBytes_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientOutBytes data.
 * get (* jmfcNamespaceRtspClientOutBytes_val_ptr ).low and (* jmfcNamespaceRtspClientOutBytes_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientOutBytes_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientOutBytes.high;
    (*jmfcNamespaceRtspClientOutBytes_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientOutBytes.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientOutBytes_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientConns
 * jmfcNamespaceRtspClientConns is subid 8 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.8
 * Description:
Number of RTSP Client sessions
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientConns data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientConns_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientConns_get(jmfcNamespaceRtspClientTable_rowreq_ctx *
                                 rowreq_ctx,
                                 u_long *
                                 jmfcNamespaceRtspClientConns_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientConns_val_ptr );


    DEBUGMSGTL(("verbose:jmfcNamespaceRtspClientTable:jmfcNamespaceRtspClientConns_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the jmfcNamespaceRtspClientConns data.
 * copy (* jmfcNamespaceRtspClientConns_val_ptr ) from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientConns_val_ptr) =
        rowreq_ctx->data.jmfcNamespaceRtspClientConns;

    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientConns_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientActiveConns
 * jmfcNamespaceRtspClientActiveConns is subid 9 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.9
 * Description:
Number of Active RTSP Client sessions
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientActiveConns data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientActiveConns_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientActiveConns_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     u_long * jmfcNamespaceRtspClientActiveConns_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientActiveConns_val_ptr );


    DEBUGMSGTL(("verbose:jmfcNamespaceRtspClientTable:jmfcNamespaceRtspClientActiveConns_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the jmfcNamespaceRtspClientActiveConns data.
 * copy (* jmfcNamespaceRtspClientActiveConns_val_ptr ) from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientActiveConns_val_ptr) =
        rowreq_ctx->data.jmfcNamespaceRtspClientActiveConns;

    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientActiveConns_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientIdleConns
 * jmfcNamespaceRtspClientIdleConns is subid 10 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.10
 * Description:
Number of Idle RTSP Client sessions
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientIdleConns data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientIdleConns_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientIdleConns_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     u_long * jmfcNamespaceRtspClientIdleConns_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientIdleConns_val_ptr );


    DEBUGMSGTL(("verbose:jmfcNamespaceRtspClientTable:jmfcNamespaceRtspClientIdleConns_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the jmfcNamespaceRtspClientIdleConns data.
 * copy (* jmfcNamespaceRtspClientIdleConns_val_ptr ) from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientIdleConns_val_ptr) =
        rowreq_ctx->data.jmfcNamespaceRtspClientIdleConns;

    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientIdleConns_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientStatus2xx
 * jmfcNamespaceRtspClientStatus2xx is subid 11 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.11
 * Description:
Number of RTSP requests that were responded
		with a 2xx status code.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientStatus2xx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientStatus2xx_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientStatus2xx_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientStatus2xx_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientStatus2xx_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientStatus2xx data.
 * get (* jmfcNamespaceRtspClientStatus2xx_val_ptr ).low and (* jmfcNamespaceRtspClientStatus2xx_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientStatus2xx_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus2xx.high;
    (*jmfcNamespaceRtspClientStatus2xx_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus2xx.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientStatus2xx_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientStatus3xx
 * jmfcNamespaceRtspClientStatus3xx is subid 12 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.12
 * Description:
Number of RTSP requests that were responded
		with a 3xx status code.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientStatus3xx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientStatus3xx_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientStatus3xx_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientStatus3xx_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientStatus3xx_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientStatus3xx data.
 * get (* jmfcNamespaceRtspClientStatus3xx_val_ptr ).low and (* jmfcNamespaceRtspClientStatus3xx_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientStatus3xx_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus3xx.high;
    (*jmfcNamespaceRtspClientStatus3xx_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus3xx.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientStatus3xx_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientStatus4xx
 * jmfcNamespaceRtspClientStatus4xx is subid 13 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.13
 * Description:
Number of RTSP requests that were responded
		with a 4xx error code.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientStatus4xx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientStatus4xx_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientStatus4xx_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientStatus4xx_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientStatus4xx_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientStatus4xx data.
 * get (* jmfcNamespaceRtspClientStatus4xx_val_ptr ).low and (* jmfcNamespaceRtspClientStatus4xx_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientStatus4xx_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus4xx.high;
    (*jmfcNamespaceRtspClientStatus4xx_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus4xx.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientStatus4xx_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcNamespaceRtspClientEntry.jmfcNamespaceRtspClientStatus5xx
 * jmfcNamespaceRtspClientStatus5xx is subid 14 of jmfcNamespaceRtspClientEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.4.4.1.14
 * Description:
Number of RTSP requests that were responded
		with a 5xx error code.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the jmfcNamespaceRtspClientStatus5xx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcNamespaceRtspClientStatus5xx_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcNamespaceRtspClientStatus5xx_get
    (jmfcNamespaceRtspClientTable_rowreq_ctx * rowreq_ctx,
     U64 * jmfcNamespaceRtspClientStatus5xx_val_ptr)
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcNamespaceRtspClientStatus5xx_val_ptr );

/*
 * TODO:231:o: |-> copy jmfcNamespaceRtspClientStatus5xx data.
 * get (* jmfcNamespaceRtspClientStatus5xx_val_ptr ).low and (* jmfcNamespaceRtspClientStatus5xx_val_ptr ).high from rowreq_ctx->data
 */
    (*jmfcNamespaceRtspClientStatus5xx_val_ptr).high =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus5xx.high;
    (*jmfcNamespaceRtspClientStatus5xx_val_ptr).low =
        rowreq_ctx->data.jmfcNamespaceRtspClientStatus5xx.low;


    return MFD_SUCCESS;
} /* jmfcNamespaceRtspClientStatus5xx_get */



/** @} */
