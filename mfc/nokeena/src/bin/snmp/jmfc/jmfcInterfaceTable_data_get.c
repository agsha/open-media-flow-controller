/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "jmfcInterfaceTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement jmfcInterfaceTable get routines.
 * TODO:240:M: Implement jmfcInterfaceTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table jmfcInterfaceTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * JUNIPER-MFC-MIB::jmfcInterfaceTable is subid 1 of jmfcInterfaces.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2636.1.2.1.2.2.1, length: 13
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement jmfcInterfaceTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param jmfcIfIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
jmfcInterfaceTable_indexes_set_tbl_idx(jmfcInterfaceTable_mib_index *tbl_idx, long jmfcIfIndex_val)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_indexes_set_tbl_idx","called\n"));

    /* jmfcIfIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->jmfcIfIndex = jmfcIfIndex_val;
    

    return MFD_SUCCESS;
} /* jmfcInterfaceTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
jmfcInterfaceTable_indexes_set(jmfcInterfaceTable_rowreq_ctx *rowreq_ctx, long jmfcIfIndex_val)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_indexes_set","called\n"));

    if(MFD_SUCCESS != jmfcInterfaceTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , jmfcIfIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != jmfcInterfaceTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* jmfcInterfaceTable_indexes_set */


/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcInterfaceEntry.jmfcIfName
 * jmfcIfName is subid 2 of jmfcInterfaceEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.2.2.1.1.2
 * Description:
The textual name of the interface.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the jmfcIfName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcIfName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param jmfcIfName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by jmfcIfName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*jmfcIfName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update jmfcIfName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
jmfcIfName_get( jmfcInterfaceTable_rowreq_ctx *rowreq_ctx, char **jmfcIfName_val_ptr_ptr, size_t *jmfcIfName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != jmfcIfName_val_ptr_ptr) && (NULL != *jmfcIfName_val_ptr_ptr));
   netsnmp_assert( NULL != jmfcIfName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcIfName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the jmfcIfName data.
 * copy (* jmfcIfName_val_ptr_ptr ) data and (* jmfcIfName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for jmfcIfName data
     */
    if ((NULL == (* jmfcIfName_val_ptr_ptr )) ||
        ((* jmfcIfName_val_ptr_len_ptr ) <
         (rowreq_ctx->data.jmfcIfName_len* sizeof(rowreq_ctx->data.jmfcIfName[0])))) {
        /*
         * allocate space for jmfcIfName data
         */
        (* jmfcIfName_val_ptr_ptr ) = malloc(rowreq_ctx->data.jmfcIfName_len* sizeof(rowreq_ctx->data.jmfcIfName[0]));
        if(NULL == (* jmfcIfName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* jmfcIfName_val_ptr_len_ptr ) = rowreq_ctx->data.jmfcIfName_len* sizeof(rowreq_ctx->data.jmfcIfName[0]);
    memcpy( (* jmfcIfName_val_ptr_ptr ), rowreq_ctx->data.jmfcIfName, rowreq_ctx->data.jmfcIfName_len* sizeof(rowreq_ctx->data.jmfcIfName[0]) );

    return MFD_SUCCESS;
} /* jmfcIfName_get */

/*---------------------------------------------------------------------
 * JUNIPER-MFC-MIB::jmfcInterfaceEntry.jmfcIfSpeed
 * jmfcIfSpeed is subid 3 of jmfcInterfaceEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2636.1.2.1.2.2.1.1.3
 * Description:
An estimate of the interface's current bandwidth in Megabits
            per second.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is GAUGE (based on perltype GAUGE)
 * The net-snmp type is ASN_GAUGE. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the jmfcIfSpeed data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param jmfcIfSpeed_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
jmfcIfSpeed_get( jmfcInterfaceTable_rowreq_ctx *rowreq_ctx, u_long * jmfcIfSpeed_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != jmfcIfSpeed_val_ptr );


    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcIfSpeed_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the jmfcIfSpeed data.
 * copy (* jmfcIfSpeed_val_ptr ) from rowreq_ctx->data
 */
    (* jmfcIfSpeed_val_ptr ) = rowreq_ctx->data.jmfcIfSpeed;

    return MFD_SUCCESS;
} /* jmfcIfSpeed_get */



/** @} */
