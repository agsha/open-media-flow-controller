/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "jmfcDiskCacheTable.h"


#include "jmfcDiskCacheTable_data_access.h"

#include "snmp_util.h"
/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table jmfcDiskCacheTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * JUNIPER-MFC-MIB::jmfcDiskCacheTable is subid 1 of jmfcDiskCaches.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2636.1.2.1.2.1.1, length: 13
*/

/**
 * initialization for jmfcDiskCacheTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param jmfcDiskCacheTable_reg
 *        Pointer to jmfcDiskCacheTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
jmfcDiskCacheTable_init_data(jmfcDiskCacheTable_registration * jmfcDiskCacheTable_reg)
{
    DEBUGMSGTL(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize jmfcDiskCacheTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* jmfcDiskCacheTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
jmfcDiskCacheTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to jmfcDiskCacheTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to jmfcDiskCacheTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up jmfcDiskCacheTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = JMFCDISKCACHETABLE_CACHE_TIMEOUT; /* seconds */
} /* jmfcDiskCacheTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before jmfcDiskCacheTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
jmfcDiskCacheTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to jmfcDiskCacheTable_container_shutdown\n");
        return;
    }

} /* jmfcDiskCacheTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement jmfcDiskCacheTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  jmfcDiskCacheTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
jmfcDiskCacheTable_container_load(netsnmp_container *container)
{
    jmfcDiskCacheTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * jmfcDiskCacheIndex(1)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/r/d/h
         */
   u_long   jmfcDiskCacheIndex;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */


    int err = 0, num_names = 0, idx = 0, rc = 0;
    tstr_array *names = NULL;
    const char *disk_name = NULL;
    node_name_t cnt_name = {0};
    node_name_t bn_name = {0};
    uint32 code = 0;
    bn_binding *binding = NULL;
    char *trigger_type = NULL;

    DEBUGMSGTL(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    err = snmp_dc_get_disks(&names);
    if (err) {
	rc = MFD_RESOURCE_UNAVAILABLE;
	bail_error(err);
    }
    num_names = tstr_array_length_quick(names);
    /* there are no disks, this is not an error */
    lc_log_debug(LOG_INFO, "num- disks: %d", num_names);
    if (0 == num_names) {
	rc = MFD_END_OF_DATA;
	goto bail;
    }

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the jmfcDiskCacheTable container.
     * loop over your jmfcDiskCacheTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    while( 1 ) {
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
	u_long jmfcNamespaceIndex = 0, jmfcDiskCacheLocation = 0;
	char dc_name[32] = "";
	node_name_t node_name = {0};
	tstring *t_diskid = NULL;
	char jmfcDiskCacheSerialNumber[256] = "",
	     jmfcDiskCacheName[256] = "",
	     jmfcDiskCacheTierType[256] = "",
	     jmfcDiskCacheState[256] = "";
	u_long jmfcDiskCacheIndex = 0,
	       jmfcDiskCacheId_len = 0,
	       jmfcDiskCacheSerialNumber_len = 0,
	       jmfcDiskCacheName_len = 0,
	       jmfcDiskCacheTierType_len = 0,
	       jmfcDiskCacheState_len = 0,
	       jmfcDiskCacheBlockSize = 0,
	       jmfcdiskCacheTotalSize = 0,
	       jmfcDiskCacheFreeSpace = 0,
	       jmfcDiskCacheBlocksUsed = 0,
	       total_blocks = 0, free_blocks = 0;

	if (idx >=  num_names) {
	    rc = MFD_END_OF_DATA;
	    break;
	}
	if (idx >= 1000) {
	    lc_log_basic(LOG_NOTICE, "reached 1000 disks");
	    rc = MFD_END_OF_DATA;
	    goto bail;
	}
	disk_name = tstr_array_get_str_quick(names, idx);

	if (NULL == disk_name) {
	    idx++;
	    continue;
	}
	jmfcDiskCacheIndex = idx;
	snprintf(dc_name, sizeof(dc_name), "dc_%d", (idx+1));
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

        /*
         * TODO:352:M: |   |-> set indexes in new jmfcDiskCacheTable rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = jmfcDiskCacheTable_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != jmfcDiskCacheTable_indexes_set(rowreq_ctx
                               , jmfcDiskCacheIndex
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "jmfcDiskCacheTable data.\n");
            jmfcDiskCacheTable_release_rowreq_ctx(rowreq_ctx);
	    idx++;
            continue;
        }

        /*
         * TODO:352:r: |   |-> populate jmfcDiskCacheTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for jmfcDiskCacheSerialNumber
     * jmfcDiskCacheSerialNumber(3)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for jmfcDiskCacheSerialNumber data
     */
        snprintf(node_name, sizeof(node_name), "/nkn/nvsd/diskcache/monitor/diskname/%s/disk_id", dc_name);
	lc_log_debug(LOG_DEBUG, "nodename: %s", node_name);

        err = mdc_get_binding_tstr (sn_get_admin_mcc(), NULL, NULL, NULL,
                &t_diskid, node_name, NULL);
        bail_error(err);

        if (t_diskid == NULL) {
            snmp_log(LOG_ERR,
                    "error in fetching disk-id for jmfcDiskCacheTable data.\n");
            jmfcDiskCacheTable_release_rowreq_ctx(rowreq_ctx);
            idx++;
            continue;
        }

        strlcpy(jmfcDiskCacheSerialNumber, ts_str_maybe_empty(t_diskid), sizeof(jmfcDiskCacheSerialNumber));
        jmfcDiskCacheSerialNumber_len = strlen(jmfcDiskCacheSerialNumber);
        rowreq_ctx->data.jmfcDiskCacheSerialNumber_len = sizeof(rowreq_ctx->data.jmfcDiskCacheSerialNumber);

    if ((NULL == rowreq_ctx->data.jmfcDiskCacheSerialNumber) ||
        (rowreq_ctx->data.jmfcDiskCacheSerialNumber_len <
         (jmfcDiskCacheSerialNumber_len* sizeof(jmfcDiskCacheSerialNumber[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    rowreq_ctx->data.jmfcDiskCacheSerialNumber_len = jmfcDiskCacheSerialNumber_len* sizeof(jmfcDiskCacheSerialNumber[0]);
    memcpy( rowreq_ctx->data.jmfcDiskCacheSerialNumber, jmfcDiskCacheSerialNumber, jmfcDiskCacheSerialNumber_len* sizeof(jmfcDiskCacheSerialNumber[0]) );
    
    /*
     * setup/save data for jmfcDiskCacheName
     * jmfcDiskCacheName(5)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
        strlcpy(jmfcDiskCacheName, dc_name, sizeof(jmfcDiskCacheName));
        jmfcDiskCacheName_len = strlen(jmfcDiskCacheName);
        rowreq_ctx->data.jmfcDiskCacheName_len  = sizeof(rowreq_ctx->data.jmfcDiskCacheName);
    /** no mapping */
    /*
     * make sure there is enough space for jmfcDiskCacheName data
     */
    if ((NULL == rowreq_ctx->data.jmfcDiskCacheName) ||
        (rowreq_ctx->data.jmfcDiskCacheName_len <
         (jmfcDiskCacheName_len* sizeof(jmfcDiskCacheName[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    rowreq_ctx->data.jmfcDiskCacheName_len = jmfcDiskCacheName_len* sizeof(jmfcDiskCacheName[0]);
    memcpy( rowreq_ctx->data.jmfcDiskCacheName, jmfcDiskCacheName, jmfcDiskCacheName_len* sizeof(jmfcDiskCacheName[0]) );
    
    /*
     * setup/save data for jmfcDiskCacheTierType
     * jmfcDiskCacheTierType(6)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
        snprintf(node_name, sizeof(node_name), "/nkn/nvsd/diskcache/monitor/diskname/%s/disk_id", dc_name);

        err = mdc_get_binding_tstr (sn_get_admin_mcc(), NULL, NULL, NULL,
                        &t_diskid, node_name, NULL);
        bail_error(err);
        snprintf(bn_name, sizeof(bn_name), "/nkn/nvsd/diskcache/monitor/disk_id/%s/disktype", ts_str(t_diskid));

        err = mdc_get_binding(sn_get_admin_mcc(), &code, NULL, false, &binding, bn_name, NULL);
        bail_error(err);

        if (binding) {
            err = bn_binding_get_str(binding, ba_value, bt_string, NULL, &trigger_type);
            bn_binding_free(&binding);
            bail_error(err);
        } else {
            snmp_log(LOG_ERR, "binding is NULL for jmfcDiskCacheTierType\n");
            rc = MFD_ERROR;
            goto bail;
        }

    /** no mapping */
        if (trigger_type == NULL) {
            snmp_log(LOG_ERR,
                     "error in fetching trigger_type for jmfcDiskCacheTable data.\n");
            jmfcDiskCacheTable_release_rowreq_ctx(rowreq_ctx);
            idx++;
            continue;
        }
        strlcpy(jmfcDiskCacheTierType, trigger_type, sizeof(jmfcDiskCacheTierType));
        jmfcDiskCacheTierType_len = strlen(trigger_type);
        rowreq_ctx->data.jmfcDiskCacheTierType_len = sizeof(rowreq_ctx->data.jmfcDiskCacheTierType);
    /*
     * make sure there is enough space for jmfcDiskCacheTierType data
     */
    if ((NULL == rowreq_ctx->data.jmfcDiskCacheTierType) ||
        (rowreq_ctx->data.jmfcDiskCacheTierType_len <
         (jmfcDiskCacheTierType_len* sizeof(jmfcDiskCacheTierType[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    rowreq_ctx->data.jmfcDiskCacheTierType_len = jmfcDiskCacheTierType_len* sizeof(jmfcDiskCacheTierType[0]);
    memcpy( rowreq_ctx->data.jmfcDiskCacheTierType, jmfcDiskCacheTierType, jmfcDiskCacheTierType_len* sizeof(jmfcDiskCacheTierType[0]) );
    
    /*
     * setup/save data for jmfcDiskCacheState
     * jmfcDiskCacheState(7)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
        snprintf(node_name, sizeof(node_name), "/nkn/nvsd/diskcache/monitor/diskname/%s/disk_id", dc_name);

        err = mdc_get_binding_tstr (sn_get_admin_mcc(), NULL, NULL, NULL,
                        &t_diskid, node_name, NULL);
        bail_error(err);
        snprintf(bn_name, sizeof(bn_name), "/nkn/nvsd/diskcache/monitor/disk_id/%s/diskstate", ts_str(t_diskid));
        err = mdc_get_binding(sn_get_admin_mcc(), &code, NULL, false, &binding, bn_name, NULL);
        bail_error(err);

        if (binding) {
            err = bn_binding_get_str(binding, ba_value, bt_string, NULL, &trigger_type);
            bn_binding_free(&binding);
            bail_error(err);
        } else {
            snmp_log(LOG_ERR, "binding is NULL for jmfcDiskCacheState\n");
            rc = MFD_ERROR;
            goto bail;
        }
    /** no mapping */
        if (trigger_type == NULL) {
            snmp_log(LOG_ERR,
                     "error in fetching trigger_type for jmfcDiskCacheTable data.\n");
            jmfcDiskCacheTable_release_rowreq_ctx(rowreq_ctx);
            idx++;
            continue;
        }
        strlcpy(jmfcDiskCacheState, trigger_type, sizeof(jmfcDiskCacheState));
        jmfcDiskCacheState_len = strlen(trigger_type);
        rowreq_ctx->data.jmfcDiskCacheState_len= sizeof(rowreq_ctx->data.jmfcDiskCacheState);
    /*
     * make sure there is enough space for jmfcDiskCacheState data
     */
    if ((NULL == rowreq_ctx->data.jmfcDiskCacheState) ||
        (rowreq_ctx->data.jmfcDiskCacheState_len <
         (jmfcDiskCacheState_len* sizeof(jmfcDiskCacheState[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    rowreq_ctx->data.jmfcDiskCacheState_len = jmfcDiskCacheState_len* sizeof(jmfcDiskCacheState[0]);
    memcpy( rowreq_ctx->data.jmfcDiskCacheState, jmfcDiskCacheState, jmfcDiskCacheState_len* sizeof(jmfcDiskCacheState[0]) );
    
    /*
     * setup/save data for jmfcDiskCacheBlockSize
     * jmfcDiskCacheBlockSize(8)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/r/d/h
     */
        snprintf(cnt_name, sizeof(cnt_name), "%s.dm2_block_size",
                tstr_array_get_str_quick(names, idx));

        jmfcDiskCacheBlockSize = nkncnt_get_uint32(cnt_name);
    /** no mapping */
    rowreq_ctx->data.jmfcDiskCacheBlockSize = jmfcDiskCacheBlockSize/1024;
    
    /*
     * setup/save data for jmfcdiskCacheTotalSize
     * jmfcdiskCacheTotalSize(9)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/r/d/h
     */
        snprintf(cnt_name, sizeof(cnt_name), "%s.dm2_total_blocks",
                tstr_array_get_str_quick(names, idx));
       /* divide by 1024 *1024 first, then multiply */
        jmfcdiskCacheTotalSize = nkncnt_get_uint32(cnt_name) * (jmfcDiskCacheBlockSize/(1024 * 1024));
    /** no mapping */
    rowreq_ctx->data.jmfcdiskCacheTotalSize = jmfcdiskCacheTotalSize;
    
    /*
     * setup/save data for jmfcDiskCacheBlocksUsed
     * jmfcDiskCacheBlocksUsed(10)/GAUGE/ASN_GAUGE/u_long(u_long)//l/A/w/e/r/d/h
     */
        snprintf(cnt_name, sizeof(cnt_name), "%s.dm2_free_blocks",
                tstr_array_get_str_quick(names, idx));

        free_blocks = nkncnt_get_uint32(cnt_name);

        snprintf(cnt_name, sizeof(cnt_name), "%s.dm2_total_blocks",
                tstr_array_get_str_quick(names, idx));

        total_blocks = nkncnt_get_uint32(cnt_name);
        jmfcDiskCacheBlocksUsed = total_blocks - free_blocks;

    /** no mapping */
    rowreq_ctx->data.jmfcDiskCacheBlocksUsed = jmfcDiskCacheBlocksUsed;
    
    /*
     * setup/save data for jmfcDiskCacheFreeSpace
     * jmfcDiskCacheFreeSpace(11)/GAUGE/ASN_GAUGE/u_long(u_long)//l/A/w/e/r/d/h
     */
        snprintf(cnt_name, sizeof(cnt_name), "%s.dm2_free_blocks",
                tstr_array_get_str_quick(names, idx));

        free_blocks = nkncnt_get_uint32(cnt_name);
        jmfcDiskCacheFreeSpace = free_blocks * (jmfcDiskCacheBlockSize /(1024 * 1024));
    /** no mapping */
    rowreq_ctx->data.jmfcDiskCacheFreeSpace = jmfcDiskCacheFreeSpace;
    
        
        /*
         * insert into table container
         */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;
	idx++;
    }

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_container_load",
               "inserted %d records\n", (int)count));

bail:
    return rc;
} /* jmfcDiskCacheTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
jmfcDiskCacheTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_container_free","called\n"));

    /*
     * TODO:380:M: Free jmfcDiskCacheTable container data.
     */
} /* jmfcDiskCacheTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
jmfcDiskCacheTable_row_prep( jmfcDiskCacheTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:jmfcDiskCacheTable:jmfcDiskCacheTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* jmfcDiskCacheTable_row_prep */

/** @} */
