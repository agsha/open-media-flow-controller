/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "jmfcInterfaceTable.h"


#include "jmfcInterfaceTable_data_access.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
#include "snmp_util.h"
/**********************************************************************
 **********************************************************************
 ***
 *** Table jmfcInterfaceTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * JUNIPER-MFC-MIB::jmfcInterfaceTable is subid 1 of jmfcInterfaces.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2636.1.2.1.2.2.1, length: 13
*/

/**
 * initialization for jmfcInterfaceTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param jmfcInterfaceTable_reg
 *        Pointer to jmfcInterfaceTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
jmfcInterfaceTable_init_data(jmfcInterfaceTable_registration * jmfcInterfaceTable_reg)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize jmfcInterfaceTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* jmfcInterfaceTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
jmfcInterfaceTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to jmfcInterfaceTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to jmfcInterfaceTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up jmfcInterfaceTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = JMFCINTERFACETABLE_CACHE_TIMEOUT; /* seconds */
} /* jmfcInterfaceTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before jmfcInterfaceTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
jmfcInterfaceTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to jmfcInterfaceTable_container_shutdown\n");
        return;
    }

} /* jmfcInterfaceTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement jmfcInterfaceTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  jmfcInterfaceTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
jmfcInterfaceTable_container_load(netsnmp_container *container)
{
    jmfcInterfaceTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * jmfcIfIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/R/d/h
         */
   long   jmfcIfIndex;

    
    /*
     * this example code is based on a data source that is a
     * text file to be read and parsed.
     */
    int err = 0, num_names = 0, idx = 0, rc = 0;
    tstr_array *names = NULL;
    bn_binding *binding = NULL;

    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_container_load","called\n"));

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    err = snmp_get_interface_names(&names);
    if (err) {
	rc = MFD_RESOURCE_UNAVAILABLE;
	bail_error(err);
    }
    num_names = tstr_array_length_quick(names);
    /* there are no disk , this is not an error */
    lc_log_debug(LOG_DEBUG, "num-interfaces : %d", num_names);
    if (0 == num_names) {
	rc = MFD_END_OF_DATA;
	goto bail;
    }

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the jmfcInterfaceTable container.
     * loop over your jmfcInterfaceTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    while( 1 ) {
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * get a line (skip blank lines)
     */
	char jmfcIfName[256] = {0};
	u_long jmfcIfName_len = 0, jmfcIfSpeed = 0;
	uint32_t ifIndex = 0;
	const char *intf_name = NULL;
	node_name_t node_name = {0};

    /*
     * parse line into variables
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

	if (idx >=  num_names) {
	    rc = MFD_END_OF_DATA;
	    break;
	}

	intf_name = tstr_array_get_str_quick(names, idx);
	if (intf_name == NULL) {
	    idx++;
	    continue;
	}

	snprintf(node_name, sizeof (node_name), 
		"/net/interface/state/%s/ifindex",intf_name);
	err = mdc_get_binding(sn_get_admin_mcc(), NULL, NULL, 
		false, &binding, node_name, NULL);
	bail_error(err);
	err = bn_binding_get_uint32(binding, ba_value, NULL, &ifIndex);
	bail_error(err);
	jmfcIfIndex = ifIndex;
        /*
         * TODO:352:M: |   |-> set indexes in new jmfcInterfaceTable rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = jmfcInterfaceTable_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
	    tstr_array_free(&names);
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != jmfcInterfaceTable_indexes_set(rowreq_ctx
                               , jmfcIfIndex
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "jmfcInterfaceTable data.\n");
            jmfcInterfaceTable_release_rowreq_ctx(rowreq_ctx);
	    idx++;
            continue;
        }

        /*
         * TODO:352:r: |   |-> populate jmfcInterfaceTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for jmfcIfName
     * jmfcIfName(2)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
	snprintf(jmfcIfName, sizeof(jmfcIfName), "%s",intf_name);
	jmfcIfName_len = strlen(jmfcIfName);
	rowreq_ctx->data.jmfcIfName_len = sizeof(rowreq_ctx->data.jmfcIfName);
    /*
     * make sure there is enough space for jmfcIfName data
     */
    if ((NULL == rowreq_ctx->data.jmfcIfName) ||
        (rowreq_ctx->data.jmfcIfName_len <
         (jmfcIfName_len* sizeof(jmfcIfName[0])))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
	tstr_array_free(&names);
        return MFD_ERROR;
    }
    rowreq_ctx->data.jmfcIfName_len = jmfcIfName_len* sizeof(jmfcIfName[0]);
    memcpy( rowreq_ctx->data.jmfcIfName, jmfcIfName, jmfcIfName_len* sizeof(jmfcIfName[0]) );
    
    /*
     * setup/save data for jmfcIfSpeed
     * jmfcIfSpeed(3)/GAUGE/ASN_GAUGE/u_long(u_long)//l/A/w/e/r/d/h
     */
	err = snmp_get_if_speed(intf_name, &jmfcIfSpeed);
	if (err) {
	    jmfcIfSpeed = 0;
	}
    /** no mapping */
    rowreq_ctx->data.jmfcIfSpeed = jmfcIfSpeed;
    
        
        /*
         * insert into table container
         */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;
	idx++;
    }

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

bail:
    DEBUGMSGT(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_container_load", "inserted %d records\n",(int) count));

    tstr_array_free(&names);
    return MFD_SUCCESS;
} /* jmfcInterfaceTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
jmfcInterfaceTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_container_free","called\n"));

    /*
     * TODO:380:M: Free jmfcInterfaceTable container data.
     */
} /* jmfcInterfaceTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
jmfcInterfaceTable_row_prep( jmfcInterfaceTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:jmfcInterfaceTable:jmfcInterfaceTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* jmfcInterfaceTable_row_prep */

/** @} */
