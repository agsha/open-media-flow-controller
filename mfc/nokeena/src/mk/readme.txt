These are makefiles and scripts to govern the entire build.
build_settings.dot -- Defines version string, Samara rev to use and
                      mfc-dmi-adapter version to use
customer.mk        -- Samara build options 
mfpcommon.mk
zmake.cfg          --  ZVM build configuration file. 
